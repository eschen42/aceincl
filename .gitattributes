# ref: https://help.github.com/en/github/using-git/configuring-git-to-handle-line-endings#per-repository-settings
# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
#eg# *.c text
#eg# *.h text

# Declare files that will always have CRLF line endings on checkout.
#eg# *.sln text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
do_clean.cmd text eol=crlf
do_copy.cmd text eol=crlf
foreach_leaf_do_copy.cmd text eol=crlf
*.md  text eol=crlf
*.txt text eol=crlf
*.vbs text eol=crlf

# Denote all files that are truly binary and should not be modified.
#eg# *.png binary
#eg# *.jpg binary
*.dia binary
*.exe binary
*.png binary
